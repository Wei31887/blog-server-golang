server:
  address: :9526
  model: debug 

db:
  dialects: psql
  host: 127.0.0.1
  port: 5432
  db: go_blog
  username: weitim
  password: tim870318
  charset: utf8

  max-idle-conns: 10
  max-open-conns: 100

mylog:
  path: ./log
  name: sys
  model: file
  format: console
  level: debug

cache:
  expier: 7200
  clearup:  600

jwt:
  signing-key: mysecret
  expires-time: 3h
  issuer: weitim
